# 合并章节拆分工作流

## 场景

在一个 write_file 调用中不小心写入了两章内容。需要拆分为各自独立的文件。

## 标准流程

1. 用 `content.find('\\n第N+1章：')` 找到分章点
2. 分别提取为独立字符串
3. 各自添加 `【本章完】`
4. 各自保存到独立文件
5. 立即核验字数：两章都要 ≥ 2500

## 注意事项

- 拆分后两个文件都必须独立通过字数核验
- **一定要在同一个 execute_code 调用中完成拆分和核验**
- 拆分后清理多余空行
